import { createStyles, makeStyles } from '@material-ui/core/styles';

const queryFilterDemoStyles = makeStyles(() =>
    createStyles({
        datumCard: {
            margin: 10,
            padding: 10,
            whiteSpace: 'pre',
        },
    }),
);

export default queryFilterDemoStyles;
