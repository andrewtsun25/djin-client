import { createStyles, makeStyles } from '@material-ui/core/styles';

const projectPageStyles = makeStyles(() =>
    createStyles({
        pageTitle: {
            margin: 10,
        },
    }),
);

export default projectPageStyles;
