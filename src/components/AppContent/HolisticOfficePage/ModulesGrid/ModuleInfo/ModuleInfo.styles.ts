import { createStyles, makeStyles } from '@material-ui/core/styles';

const moduleInfoStyles = makeStyles(() =>
    createStyles({
        architecturePaper: {
            padding: 10,
            margin: 10,
        },
    }),
);

export default moduleInfoStyles;
