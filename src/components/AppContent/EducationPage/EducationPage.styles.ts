import {createStyles, makeStyles} from "@material-ui/core/styles";

const educationPageStyles = makeStyles(() => createStyles({
    pageTitle: {
        textAlign: "center"
    }
}));

export default educationPageStyles;
